// The sign-in screen renders in under a tenth of a second and owns none of the
// fleet chrome, so the fleet skeleton above it would be a flash of furniture
// that does not belong to this page. Nothing is the right placeholder here.
export default function LoginLoading() {
  return null;
}
